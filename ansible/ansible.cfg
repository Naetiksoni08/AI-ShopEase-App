[defaults]
inventory        = inventory.ini
remote_user      = ec2-user
private_key_file = ~/.ssh/shopease-key.pem
host_key_checking = False
retry_files_enabled = False
interpreter_python = auto_silent